# Enable this to be able to upgrade a store from 1.4 -> 1.5 or 1.4 -> 1.6
allow_store_upgrade=true

# Enable this to specify a parser other than the default one. 1.5, 1.6, 1.7 are available
#cypher_parser_version=1.6

# Keep logical logs, helps debugging but uses more disk space, enabled for
# legacy reasons To limit space needed to store historical logs use values such
# as: "7 days" or "100M size" instead of "true"
keep_logical_logs=10 days

# Autoindexing

# Enable auto-indexing for nodes, default is false
node_auto_indexing=false

# The node property keys to be auto-indexed, if enabled
#node_keys_indexable=

# Enable auto-indexing for relationships, default is false
#relationship_auto_indexing=true

# The relationship property keys to be auto-indexed, if enabled
#relationship_keys_indexable=name,age

# Configure caching type
# cache_type=gcr

# HA Setup
# ha.discovery.enabled=false
# ha.pull_interval=0
# ha.tx_push_factor=2
# ha.server=10.100.41.57:6002
# ha.cluster_server=10.100.41.57:5001
# ha.server_id=10553
# ha.initial_hosts=10.100.41.57:5001,10.100.40.221:5001,10.100.41.155:5001
# ha.heartbeat_interval=2s
# ha.heartbeat_timeout=20s